#include <string>
#include <vector>

using namespace std;

int solution(int num, int n) {
    int answer = 0;
    
    if(num % n == 0){//배수 이기 때문에 나머지가 0 이 되어야한다.
        answer = 1;
    }
    else{
        answer = 0;
    }
    
    return answer;
}
