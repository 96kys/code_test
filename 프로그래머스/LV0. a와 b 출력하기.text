#include <stdio.h>

int main(void) {
    
    //입력 받을 배열 선언
    int a;
    int b;
    
    //값 입력
    scanf("%d %d", &a, &b);
    
    //값 출력
    printf("a = %d\n", a);
    printf("b = %d", b);
    
    
    return 0;
}
